{% extends 'uniMarcaBundle::layout.html.twig' %}

{% block central %}
 
    <div class="detalleNot">
        <div class="LineaFormulario"></div>

        <h1 class="centrado">{{entity.titulo}}</h1>

        <div class="LineaFormulario"></div>

        <div class="LineaFormulario centrado">
            <img src="{{ asset('bundles/unimarca/img/'~ entity.foto  ~ '') }}" width="25%" heigth="25%"/>
        </div>

        <div class="LineaFormulario">
            <h3 class="izquierda separarIzq20">{{entity.autor}} {{ entity.fechaPub|date('Y/m/d') }}</h3>
        </div>


        <div class="LineaFormulario">
            <p>{{entity.contenido|raw|nl2br}}</p>
        </div>

        <div class="LineaFormulario">
            <h3 class="izquierda separarIzq20">Comentarios</h3>
        </div>
        
        {% for coment in entity.comentarioss %}
        <div class="LineaFormulario">
                      
            <p class="negrita">({{ coment.fecha|date('Y-m-d') }} - {{ coment.nick }}) {{ coment.tit }}</p>  
            <p>{{ coment.descrip }}</p>
               
        </div>
        {% endfor %}

        <div class="LineaFormulario"></div>
        <div class="LineaFormulario"></div>

         <a href="{{path('noticias' )}}"> Volver atras</a>  
         <a href="{{ path('noticias_edit', { 'id': entity.id }) }}">Edit</a>
         <a href="{{ path('comentarios_new') }}">Añadir comentario</a>
    </div>
{% endblock %}
